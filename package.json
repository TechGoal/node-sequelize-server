{
  "name": "node-sequelize-serer",
  "version": "1.0.0",
  "description": "Basic micro framework.",
  "main": "index.js",
  "scripts": {
    "lint": "npx eslint ./",
    "dev": "export NODE_ENV=development || set NODE_ENV=development && nodemon ./index.js",
    "prod": "export NODE_ENV=production || set NODE_ENV=production  && node ./index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "pg": "^8.0.3",
    "pg-hstore": "^2.3.3",
    "sequelize": "^5.21.7",
    "sequelize-cli": "^5.5.1"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-google": "^0.14.0"
  }
}
